# Dependencies
frontend/node_modules/
frontend/dist/
secrets/test_secret.txt

# Python
__pycache__/
*.pyc

# CLI-managed relay/hook-server processes (pids + logs from `cli.py start`)
agent/.run/

# Claude Code local/personal settings (settings.json is project-shared, keep that one)
.claude/settings.local.json

# Environment files
.env
.env.*
!.env.example

# Runtime policy state -- edited live from the UI's Policy tab, seeded
# with sensible defaults on first run (see policy.py's _ensure_policies_file)
agent/policies.json

# Production notes
agenttrail-prd-v2.md