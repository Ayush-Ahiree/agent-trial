anthropic>=0.34.0
openai>=1.40.0
opentelemetry-api>=1.25.0
opentelemetry-sdk>=1.25.0
opentelemetry-exporter-otlp-proto-grpc>=1.25.0
websockets>=12.0

# Hosted (multi-tenant) backend -- agent/main.py and friends.
# The local self-host path (hook_server.py/ws_relay.py/cli.py) only needs
# the deps above and stays stdlib-only otherwise.
fastapi>=0.115.0
uvicorn[standard]>=0.30.0
asyncpg>=0.29.0
PyJWT>=2.9.0
cryptography>=42.0.0  # PyJWT needs this to verify Supabase's ES256-signed JWTs (asymmetric signing keys)
python-dotenv>=1.0.0  # loads agent/.env locally; no-op on Fly, which sets these via `flyctl secrets`
